server {
  listen 80;
  listen [::]:80 ipv6only=on default_server;
  server_name prycely.com www.prycely.com;
  root /home/prometheus/prycely/current/public;

  passenger_enabled on;
  passenger_app_env production;

  location /cable {
    passenger_app_group_name prycely_websocket;
    passenger_force_max_concurrent_requests_per_process 0;
  }

  # Allow uploads up to 100MB in size
  client_max_body_size 100m;

  location ~ ^/(assets|packs) {
    expires max;
    gzip_static on;
  }
}

##########################################################################################
##########################################################################################
https://www.thesslstore.com/knowledgebase/ssl-install/nginx-ssl-installation/
##########################################################################################
##########################################################################################


  GNU nano 4.8                                                                          /etc/nginx/sites-enabled/prycely                                                                                     
server {
    listen 80;
    listen [::]:80 ipv6only=on default_server;
    server_name prycely.com www.prycely.com;
    root /home/prometheus/prycely/current/public;
    rewrite ^/(.*) https://prycely.com/$1 permanent;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name prycely.com www.prycely.com;
    root /home/prometheus/prycely/current/public;

    ssl on;
    ssl_certificate /etc/nginx/ssl/tlscert.crt;
    ssl_certificate_key /etc/nginx/ssl/private.key;
    # ssl_certificate /home/prometheus/ca_bundle.chained.crt;
    # ssl_certificate_key /home/prometheus/private.key;
    # ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
    # ssl_prefer_server_ciphers on;
    # ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';

    # ssl_certificate /etc/letsencrypt/csr/0000_csr-certbot.pem;
    # ssl_certificate_key /etc/letsencrypt/keys/0000_key-certbot.pem;
    # include /etc/letsencrypt/options-ssl-nginx.conf;
    # ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    passenger_enabled on;
    passenger_app_env production;

    location /cable {
        passenger_app_group_name prycely_websocket;
        passenger_force_max_concurrent_requests_per_process 0;
    }
    # Allow uploads up to 100MB in size
    client_max_body_size 100m;
    location ~ ^/(assets|packs) {
        expires max;
        gzip_static on;
    }
}

