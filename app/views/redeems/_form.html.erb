<%= form_with(model: redeem) do |form| %>
  <% if redeem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(redeem.errors.count, "error") %> prohibited this redeem from being saved:</h2>

      <ul>
        <% redeem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :invite_id %>
    <%= form.number_field :invite_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
