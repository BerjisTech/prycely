<div class="groups_holder">
  <% @groups.each do |group| %>
    <%= link_to group_path(group.id), class: "group" do %>
      <span class="group_dp"><%= group.name[0] %></span>
      <span class="group_name"><%= group.name %></span>
      <span><%= group.currency %></span>
    <% end %>
  <% end %>
</div>
