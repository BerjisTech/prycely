<table class="table table-hover">
  <thead>
    <tr>
      <th>User</th>
      <th>Amount</th>
      <th>Type</th>
      <th>Category</th>
      <th>Date</th>
    </tr>
  </thead>
  <tbody>
    <% @transactions.map do |transaction| %>
      <tr>
        <td>
          <%= link_to Account.full_names(transaction.user_id), member_path(Member.find_by(user_id: transaction.user_id).id) %>
        </td>
        <td>
          <span class="fw-bold
              <% if transaction.transaction_type == 1 %>
               text-success
              <% else %>
                text-danger
              <% end %>">
            <%= number_with_precision(Transact.amount_from_cents(transaction.amount), precision: 2, delimiter: ",") %>
          </span>
        </td>
        <td><%= Paymentcategory.category_name[transaction.transaction_type] %></td>
        <td><%= Paymentcategory.find(transaction.category).name %></td>
        <td>
          <%= transaction.updated_at.strftime("%b, #{transaction.updated_at.day.ordinalize} %Y") %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
