<script type="text/javascript">
  // Code used to add Todo Tasks
  jQuery(document).ready(function($)
  {
    function escapeHtml(text) {
        var map = {
            '&': '&amp;',
            '<': '&lt;',
            '>': '&gt;',
            '"': '&quot;',
            "'": '&#039;'
        };
        return text.replace(/[&<>"']/g, (m) => { return map[m]; });
    }
    var $todo_tasks = $("#todo_tasks");
    $todo_tasks.find('input[type="text"]').on('keydown', (ev) => {
        if(ev.keyCode == 13) {
            ev.preventDefault();
            if($.trim($(this).val()).length) {
                var $todo_entry = $('<li><div class="checkbox checkbox-replace color-white"><input type="checkbox" /><label>'+escapeHtml($(this).val())+'</label></div></li>');
                $(this).val('');
                $todo_entry.appendTo($todo_tasks.find('.todo-list'));
                $todo_entry.hide().slideDown('fast');
                replaceCheckboxes();
            }
        }
    });
  });
</script>

<div class="sidebar-menu">
  <div class="sidebar-menu-inner">
    <header class="logo-env">
      <div class="logo">
        <a href="https://prycely.com/dashboard/">
          <img src="https://assets.prycely.com/images/d6f18dcdfc48ef9c283fa8e68a5c7a9e.png" class="img img-rounded" alt="" style="background: #fff; width: 40px;"/>
        </a>
      </div>
      <div class="sidebar-collapse"> <a href="#" class="sidebar-collapse-icon"> <i
                                class="material-icons">menu</i> </a> </div>
      <div class="sidebar-mobile-menu visible-xs"> <a href="#" class="with-animation"> <i
                                class="material-icons">menu</i> </a> </div>
    </header>

    <div class="sidebar-user-info">
      <div class="sui-normal"> <a href="#" class="user-link">
          <%= image_tag Account.mine(current_user.id).image.variant(resize: "500x500"), width:"100", alt:"", class:"img-rounded"%>
          <span>Welcome,</span>
          <strong><%= Account.mine(current_user.id).first_name %></strong> </a>
      </div>
      <div class="sui-hover inline-links animate-in">
        <%= link_to groups_path do %>
          <i class="material-icons">dashboard</i>
          Groups
        <% end %>
        <%= link_to wallets_path do %>
          <i class="material-icons">account_balance_wallet</i>
          Wallets
        <% end %>
        <%= link_to account_path(Account.where(user_id: current_user.id).last.id), method: :get do %>
          <i class="material-icons">manage_accounts</i>
          Account
        <% end %>
        <span class="close-sui-popup">×</span>
      </div>
    </div>

    <ul id="main-menu" class="main-menu">
      <li>
        <%= link_to dashboard_path do %>
          <i class="material-icons">dashboard</i>
          <span class="title">Dashboard</span>
        <% end %>
      </li>
      <li class="has-sub <% if controller_name == "groups" %>opened<% end %>">
        <%= link_to groups_path do %>
          <i class="material-icons">account_balance</i>
          <span class="title">
            Group 
            <% if session[:current_group] %>
              (<%= Group.find(session[:current_group]).name %>)
            <% end %>
          </span>
        <% end %>
        <ul>
          <!--li>
            <% Group.mine(current_user.id).map do |group| %>
              <%= link_to group_path(group.group_id) do %>
                <i class="entypo-newspaper"></i>
                <span class="title"><%= group.name %></span>
              <% end %>
            <% end %>
          </li-->
          <% if session[:current_group] %>
            <li>
              <%= link_to group_path(session[:current_group]) do %>
                <i class="entypo-flow-parallel"></i><span class="title">Overview</span>
              <% end %>
            </li>
          <% end %>
          <li>
            <%= link_to members_path do %>
              <i class="entypo-flow-parallel"></i><span class="title">Members</span>
            <% end %>
          </li>
          <li>
            <%= link_to projects_path do %>
              <i class="entypo-flow-parallel"></i><span class="title">Projects</span>
            <% end %>
          </li>
          <li>
            <%= link_to loans_path do %>
              <i class="entypo-flow-parallel"></i><span class="title">Loans</span>
            <% end %>
          </li>
          <li>
            <%= link_to assets_path do %>
              <i class="entypo-flow-parallel"></i><span class="title">Assets</span>
            <% end %>
          </li>
          <li>
            <%= link_to liabilities_path do %>
              <i class="entypo-flow-parallel"></i><span class="title">Liabilities</span>
            <% end %>
          </li>
        </ul>
      </li>
      <li class="has-sub <% if controller_name == "wallets" %>opened<% end %>">
        <%= link_to wallets_path do %>
          <i class="material-icons">account_balance</i>
          <span class="title">Wallets</span>
        <% end %>
        <ul>
          <li>
            <% Wallet.mine(current_user.id).map do |wallet| %>
              <%= link_to wallet_path(wallet.id) do %>
                <i class="entypo-newspaper"></i>
                <span class="title"><%= wallet.currency.upcase %></span>
              <% end %>
            <% end %>
          </li>
        </ul>
      </li>
    </ul>
  </div>
</div>
