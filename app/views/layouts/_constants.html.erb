<script>
  var base_url = '<%= root_url %>'
  var page_title = '<%= controller_name %>'
  <% if session[:current_group] %>
  var active_group = '<%= session[:current_group] %>'
  <% end %>
  <% if user_signed_in? %>
  var account_id = '<%= Account.find_by(user_id: current_user.id).id %>'
  var user_id = '<%= current_user.id %>'
  <% end %>
</script>
